// 常量与 Store 字段键名（与原意保持一致）
export const API_KEY_FIELD = 'api_key';
export const USER_INFO_FIELD = 'user_info';
export const SHORTCUTS_FIELD = 'shortcuts';

// 业务限制（与 UI 文案一致）
export const SHORTCUTS_MAX = 5;
export const NAME_MAX_LEN = 50;
export const PROMPT_MAX_LEN = 2000;

// UI
export const PAGE_HEIGHT = 420;
